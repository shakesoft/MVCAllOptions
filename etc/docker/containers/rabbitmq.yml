volumes:
  mvcalloptions_rabbitmq:
networks:
  mvcalloptions:
    external: true

services:
  rabbitmq:
    container_name: rabbitmq
    image: rabbitmq:3.12.7-management-alpine
    volumes:
      - mvcalloptions_rabbitmq:/var/lib/rabbitmq
    networks:
      - mvcalloptions
    ports:
      - "15672:15672"
      - "5672:5672"
